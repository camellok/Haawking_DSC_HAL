# Haawking DSC HAL Roadmap

> 最近更新：2026-09-09
>
> 当前阶段：P1 - Platform骨架与HXS320F28002x CpuTimer验收
>
> 当前状态：CpuTimer实现完成；platform参考已调整为整板组合入口；等待宿主编译与上板验证

## 路线目标

本仓库面向完整的Haawking DSC外设HAL，不以CpuTimer或CAN为范围终点。路线优先覆盖C2000Ware类实时控制项目最常用的CpuTimer、ePWM、ADC、CLA、CAN、SCI和I2C，再补齐保护、采集、位置、通信和系统支撑外设。

公共API只表达跨芯片目标可保持一致的外设能力。芯片差异由`source/<target>/`实现，实例选择、时钟树、pinmux、中断路由、DMA/XBAR、对象存储和跨外设组合由platform管理。

## 状态定义

| 状态 | 含义 |
|---|---|
| 未开始 | 尚未进入接口设计 |
| 设计中 | 正在确定公共能力、边界和失败语义 |
| 实现中 | 公共接口或目标源码正在开发 |
| 待验证 | 实现完成，等待宿主编译或上板测试 |
| 已完成 | 本阶段验收完成，并已更新兼容记录 |
| 阻塞 | 存在已记录且无法继续的外部条件 |

## 总体阶段

| 阶段 | 核心内容 | 状态 | 版本目标 |
|---|---|---|---|
| P0 | 仓库、协作规则、GitHub治理和自动检查 | 已完成 | 仓库基线 |
| P1 | 通用platform骨架与CpuTimer | 待验证 | `v0.1.0` |
| P2 | ePWM波形、同步、触发和保护基础 | 未开始 | 后续minor版本 |
| P3 | ADC采样、SOC、PPB和中断 | 未开始 | 后续minor版本 |
| P4 | CLA任务控制与CPU/CLA数据边界 | 未开始 | 后续minor版本 |
| P5 | Classic CAN统一接口与目标实现迁移 | 未开始 | 后续minor版本 |
| P6 | SCI串行通信 | 未开始 | 后续minor版本 |
| P7 | I2C控制器与目标设备通信 | 未开始 | 后续minor版本 |
| P8 | GPIO、CMPSS、DAC、SPI、eCAP、eQEP和DMA | 未开始 | 按需求分批发布 |
| P9 | FSI及其他芯片特定外设 | 未开始 | 按目标能力立项 |
| P10 | 第二芯片平台与跨平台兼容验证 | 未开始 | `v1.0.0`前完成 |

## P0：仓库基线

已于2026-09-08完成公开仓库初始化、协作规范、项目主页、Issues、外设与平台标签、`CpuTimer v0.1.0` milestone，以及`main`分支的Pull Request和`policy`静态检查规则。Wiki已关闭，GitHub Pages保持未配置。

## P1：Platform骨架与CpuTimer

Platform工作：

- 以`platform.h`和`PLATFORM_init()`作为整板组合入口；
- 用`platform_config.h`集中展示板级时钟参数，不向HAL公共头文件泄漏DriverLib；
- platform持有HAL对象，统一安排实例、时钟源、中断注册和PIE ACK；
- CpuTimer0提供1 ms platform时间基准，CpuTimer2提供自由运行原始时间戳，CpuTimer1保持未分配；
- 后续ePWM、ADC、CLA和通信模块按依赖顺序加入同一platform组合层，不为每个HAL复制一套独立“platform”。

CpuTimer已实现：

- 生命周期、物理周期、物理分频和三种调试仿真模式；
- 启动、续跑、停止、重装、计数读取、溢出状态和中断门控；
- HXS320F28002x DriverLib目标实现。

发布`v0.1.0`前必须完成：

- 宿主工程编译和链接；
- 空指针、非法实例、零周期、超范围周期和非法分频测试；
- 初始化后停止、中断关闭、重装和旧标志清除测试；
- `start()`、`resume()`、`stop()`和`reload()`生命周期测试；
- Timer0 1 ms中断周期、Timer2自由运行与回绕测试；
- 三种调试仿真模式测试；
- 验证记录、兼容矩阵和CHANGELOG更新。

## P2：ePWM HAL

公共能力计划：

- 时基周期、计数模式、相位和时钟分频；
- 比较寄存器与影子装载策略；
- Action Qualifier输出行为；
- Dead-Band极性、延时和旁路；
- ePWM同步输入、同步输出和相位装载；
- ADC SOC与外设中断事件选择；
- Trip Zone一次触发、周期触发和软件强制；
- 高频更新所需的原子配置或受控提交语义。

Platform负责ePWM实例与GPIO复用、同步链、XBAR、故障源、ADC触发关系和安全启动顺序。首轮验收覆盖频率/占空比、中心对齐、互补死区、多模块同步、SOC时刻、Trip响应和调试暂停行为。

## P3：ADC HAL

公共能力计划：

- 分辨率、信号模式、时钟和转换器上电；
- SOC通道、触发源和采样窗口配置；
- 单次/连续触发与原始结果读取；
- ADC中断源、连续模式和溢出处理；
- PPB偏置、限值和时间戳等可跨目标表达的能力；
- 校准值应用的接口边界和失败语义。

Platform负责模拟引脚、参考源、SOC到ePWM事件映射、中断路由、DMA目标和多ADC同步。验收覆盖静态输入、满量程边界、采样窗口、ePWM触发序列、过采样/连续采样、中断溢出和PPB事件。

## P4：CLA HAL

公共能力计划：

- CLA实例初始化和任务启停状态；
- 任务触发源映射、软件触发和完成状态；
- CPU与CLA消息RAM初始化和数据所有权；
- CLA任务向量绑定的目标接口；
- 后台任务、任务优先级和软件断点能力按芯片支持声明。

CLA程序、链接命令文件、段放置、数学库选择和算法实现继续由宿主工程负责。Platform负责触发源、存储区、任务入口和中断路由。验收需要同时证明CPU侧编译链接、CLA任务执行、数据一致性、触发时序和异常任务状态。

## P5：Classic CAN HAL

以训练仓库中冻结后的单一接口为输入重新整理，不直接搬运多套Lab演进副本。

公共能力计划：

- 位时序和正常、静默、环回等控制模式；
- 接收过滤器和发送/接收消息对象配置；
- 标准帧、扩展帧、远程帧及DLC校验；
- 非阻塞发送、接收和消息对象状态；
- 控制器状态、错误计数、Bus-Off和恢复语义；
- 中断原因读取、事件清除和上层事件交接。

Platform负责CAN实例、GPIO、时钟、中断注册、对象资源分配和收发缓冲存储；协议栈负责CANopen等协议。验收覆盖过滤、并发消息对象、满载收发、错误注入、Bus-Off恢复和中断边界。

## P6：SCI HAL

公共能力计划：

- 波特率、数据位、停止位和奇偶校验；
- 发送、接收、忙状态和错误状态；
- FIFO阈值、复位和溢出处理；
- 中断或DMA模式所需的事件接口；
- 调试暂停和内部回环能力。

Platform负责SCI实例、GPIO、外设时钟、中断/DMA路由和缓冲区；命令行、Modbus及其他帧协议留在上层。验收覆盖波特率误差、回环、连续收发、FIFO边界、帧错误、奇偶校验错误和接收溢出。

## P7：I2C HAL

公共能力计划：

- 控制器和目标设备模式配置；
- 7位/10位地址、收发方向和重复起始；
- 阻塞基础事务与可扩展的非阻塞事务状态；
- FIFO、中断、NACK、仲裁丢失和总线忙状态；
- 超时与总线恢复的职责边界。

Platform负责I2C实例、开漏GPIO、电气上拉、时钟、中断和DMA资源。具体传感器或EEPROM协议属于设备驱动。验收覆盖寄存器读写、重复起始、NACK、超时、总线占用、仲裁丢失和恢复流程。

## P8：通用与实时控制支撑外设

按实际项目依赖拆分独立Issue和版本：

| 模块 | 计划边界 |
|---|---|
| GPIO | 数字输入输出、锁定和资格配置；pinmux与板级信号命名仍由platform负责 |
| CMPSS | 比较器、DAC阈值、滤波和锁存；与ePWM Trip/XBAR的连接由platform负责 |
| DAC | 参考源、输出值、同步装载和上电状态 |
| SPI | 帧格式、时钟、FIFO和事务状态；片选策略与具体器件协议由platform/设备驱动负责 |
| eCAP | 边沿捕获、时间戳、单次/连续模式和事件状态 |
| eQEP | 位置、方向、单位定时、捕获和索引事件 |
| DMA | 通道传输配置、触发和状态；缓冲区所有权及跨外设路由由platform负责 |

## P9：扩展外设

根据芯片实际能力和客户项目优先级评审FSI、PMBus、LIN、SDFM、HRPWM、CLB、CRC、看门狗及片上通信外设。芯片独有能力不得强行进入所有目标都必须实现的公共API；必要时通过能力查询、可选接口或目标扩展头文件表达。

## P10：多平台与1.0准备

- 增加第二个芯片族目标实现并验证源码选择机制；
- 对每个公共模块建立支持矩阵和功能差异记录；
- 验证同一platform组合多个HAL时的初始化顺序和资源冲突处理；
- 完成API命名、一致性、错误语义、并发和中断上下文审查；
- 冻结首批稳定API，并在证据完整后评审`v1.0.0`。

## 外设准入与发布规则

每个模块的Issue必须说明使用场景、目标芯片、公共能力、目标特性、platform资源、失败语义、实时约束和验收方法。模块只有在接口、目标实现、宿主编译、上板证据、文档和兼容记录全部完成后，才能进入发布版本。
